import * as React from 'react';
import { TooltipProps } from '../../overlays/Tooltip';
import { BaseRatingProps } from './BaseRating';
import { Icon } from '@epam/uui-core';
export interface RatingProps extends BaseRatingProps<number> {
    filledStarIcon?: Icon;
    emptyStarIcon?: Icon;
    hideTooltip?: boolean;
    hint?: (value: number) => string;
    Tooltip?: React.ComponentType<TooltipProps>;
}
export declare class Rating extends React.Component<RatingProps> {
    getFilledStarsWidth: (rating: number, markWidth: number) => number;
    hidingTooltip: (rating: number) => null | string;
    getEmptyStars: (rating: number) => JSX.Element[];
    getFilledStars: (rating: number) => JSX.Element[];
    renderRating: (rating: number, markWidth: number) => JSX.Element;
    render(): JSX.Element;
}
//# sourceMappingURL=Rating.d.ts.map