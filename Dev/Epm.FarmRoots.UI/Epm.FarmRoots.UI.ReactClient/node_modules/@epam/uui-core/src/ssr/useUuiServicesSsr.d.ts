import { UseUuiServicesProps } from '../hooks/useUuiServices';
export interface IContextProviderSsrProps<TApi, TAppContext> extends UseUuiServicesProps<TApi, TAppContext> {
    router: any;
}
/**
 * This hook creates UUI context compatible with Next.js pages router
 *
 * @example
 * const { services } = useUuiServicesSsr({ ... });
 * <UuiContext.Provider value={ services }> ... </UuiContext.Provider>
 * // And then, use it in code like this:
 * const services = useUuiContext<TApi, AppContextType>();
 *
 * @param props
 */
export declare function useUuiServicesSsr<TApi, TAppContext>(props: IContextProviderSsrProps<TApi, TAppContext>): {
    services: import("index").CommonContexts<TApi, TAppContext>;
};
//# sourceMappingURL=useUuiServicesSsr.d.ts.map